<div class="container mx-auto px-4 py-8 max-w-4xl">
  <nav class="mb-4 text-sm text-gray-600">
    <%= link_to "Trang chủ", root_path, class: "hover:text-blue-600" %> /
    <span class="text-gray-900"><%= @slug.humanize %></span>
  </nav>

  <article>
    <h1 class="text-4xl font-bold mb-8"><%= @slug.humanize %></h1>
    
    <!-- Placeholder content - replace with actual page content from database -->
    <div class="prose max-w-none">
      <p class="text-gray-600">Nội dung trang này sẽ được cập nhật sau.</p>
    </div>
  </article>
</div>

